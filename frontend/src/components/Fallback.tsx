import ProjectItemSkeleton from './ProjectItemSkeleton.tsx';

const Fallback = () => {
  return (
    <div className='px-8'>
      <ProjectItemSkeleton />
    </div>
  );
};

export default Fallback;
